"use client";

import React from "react";
import Hero from "../../components/Hero"; // or from "../../components/Hero" if you put it in a shared folder

export default function HeroTestPage() {
  return (
    <main>
      <Hero />
    </main>
  );
}
